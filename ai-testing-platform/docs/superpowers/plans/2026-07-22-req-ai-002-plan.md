# REQ-AI-002 Implementation Plan

**Goal:** Activate `dependency_count` and `last_modified_days` factors in `DefectPredictor._calculate_factors()` with rebalanced weights.

---

## File Structure

```
Modify: src/defect_predictor/predictor.py
Modify: tests/test_defect_predictor/test_defect_predictor.py
```

No new files needed.

---

## Task 1: Update RISK_WEIGHTS

**Step 1:** Change weights in `predictor.py`:

```python
RISK_WEIGHTS = {
    "complexity": 0.22,
    "churn": 0.22,
    "coverage_gap": 0.18,
    "bug_history": 0.18,
    "size": 0.08,
    "dependency": 0.07,
    "staleness": 0.05,
}
```

**Step 2:** Add factor formulas in `_calculate_factors`:

```python
dependency_score = min(100.0, metrics.dependency_count * 5.0)
staleness_score = min(100.0, metrics.last_modified_days / 3.65)
```

Add to return dict: `"dependency"` and `"staleness"`.

**Step 3:** Add recommendations:

```python
if factors["dependency"] > 50:
    recs.append(...)
if factors["staleness"] > 50:
    recs.append(...)
```

**Step 4:** Update `model_version` to `"rule-based-v1.1"`.

---

## Task 2: Update tests

**Step 1:** Update TC-PRD-003 factor keys assertion to include `"dependency"` and `"staleness"`.

**Step 2:** Add TC-PRD-013 model version test.

**Step 3:** Add TC-PRD-014 high dependency test.

**Step 4:** Add TC-PRD-015 stale code test.

---

## Task 3: Verify

```bash
pytest tests/test_defect_predictor/ -v
```

Expected: All 15+ tests PASS. Existing 13 tests not broken.
