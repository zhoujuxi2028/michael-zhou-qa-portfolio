# ==================== Application Configuration ====================
# IWSVA server URL
BASE_URL=https://10.206.201.9:8443

# Login credentials
USERNAME=admin
PASSWORD=111111

# ==================== SSH Configuration ====================
# For backend verification (optional)
SSH_HOST=10.206.201.9
SSH_PORT=22
SSH_USERNAME=root
SSH_PASSWORD=

# ==================== Browser Configuration ====================
# Browser choice: chrome, firefox, edge
BROWSER=chrome

# Headless mode: true, false
HEADLESS=false

# Browser window size
BROWSER_WIDTH=1920
BROWSER_HEIGHT=1080

# ==================== WebDriver Version Management ====================
# Control ChromeDriver/GeckoDriver versions for reproducible test environments
#
# 3 Modes (in priority order):
# 1. Explicit Path (CI/CD): Use pre-installed driver
#    CHROMEDRIVER_PATH=/usr/local/bin/chromedriver
#
# 2. Version Lock (Development): Pin specific version
#    CHROMEDRIVER_VERSION=145.0.7054.8
#
# 3. Auto-detect (Default): Use system Chrome version with cache
#    (Leave both empty - fallback mode)

# ChromeDriver configuration
# CHROMEDRIVER_PATH=/path/to/chromedriver        # Priority 1: Explicit path
# CHROMEDRIVER_VERSION=145.0.7054.8              # Priority 2: Version lock
CHROMEDRIVER_CACHE_VALID_DAYS=7                  # Cache duration (default: 7 days)

# GeckoDriver (Firefox) configuration
# GECKODRIVER_PATH=/path/to/geckodriver          # Priority 1: Explicit path
# GECKODRIVER_VERSION=v0.34.0                    # Priority 2: Version lock
GECKODRIVER_CACHE_VALID_DAYS=7                   # Cache duration (default: 7 days)

# EdgeDriver configuration (if needed)
# EDGEDRIVER_PATH=/path/to/msedgedriver
# EDGEDRIVER_VERSION=145.0.2054.8

# ==================== How to Find ChromeDriver Version ====================
# Method 1: Check current Chrome version
#   google-chrome --version                      # Linux
#   /Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --version  # macOS
#
# Method 2: Find matching ChromeDriver version
#   Visit: https://googlechromelabs.github.io/chrome-for-testing/
#   Look for "Stable" channel version matching your Chrome
#
# Example: Chrome 145.0.6533.89 → ChromeDriver 145.0.7054.8

# ==================== Test Configuration ====================
# Expected kernel version for verification
TARGET_KERNEL_VERSION=5.14.0-427.24.1.el9_4.x86_64

# Test environment: dev, qa, staging, production
TEST_ENV=qa

# ==================== Retry Configuration ====================
# Number of retries for failed tests
MAX_RETRIES=2

# Delay between retries (seconds)
RETRY_DELAY=1

# ==================== Logging Configuration ====================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ==================== Video Recording (Optional) ====================
# Enable video recording of test execution
ENABLE_VIDEO=false

# ==================== Notes ====================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to git (it's in .gitignore)
# 3. All values are examples - replace with your actual configuration
# 4. SSH configuration is optional - only needed for backend verification tests
