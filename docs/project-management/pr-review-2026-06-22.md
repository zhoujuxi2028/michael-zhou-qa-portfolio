# Open PR Review Summary (2026-06-22)

## 可安全合并的 PRs（CI 全绿）

| PR | 标题 | 类型 | CI 状态 |
|----|------|------|---------|
| #378 | bump prettier 3.8.3→3.8.4 (microservice) | patch | ✅ All green |
| #377 | bump prettier 3.8.3→3.8.4 (perf) | patch | ✅ All green |
| #376 | bump eslint 10.4.1→10.5.0 (microservice) | minor | ✅ All green |
| #375 | bump better-sqlite3 12.10.0→12.10.1 (perf) | patch | ✅ All green |
| #374 | bump better-sqlite3 12.10.0→12.10.1 (microservice) | patch | ✅ All green |
| #372 | bump cypress 15.16.0→15.17.0 (cicd-demo) | minor | ✅ All green |
| #370 | bump hashicorp/aws 6.49.0→6.50.0 (terraform) | minor | ✅ All green |
| #369 | bump cypress 15.16.0→15.17.0 (iwsva) | minor | ✅ All green |

### 批量合并命令

```bash
for PR in 375 377 378 374 376 372 370 369; do
  gh pr merge $PR --squash --auto
done
```

---

## 需关闭的 PRs（Breaking Changes）

| PR | 标题 | 失败原因 | 建议 |
|----|------|----------|------|
| #371 | bump eslint 8.57.1→10.5.0 (perf) | ESLint 10 要求 `eslint.config.js` flat config，项目仍使用 `.eslintrc.*` | 关闭，需专项迁移 |
| #373 | bump eslint 8.57.1→10.5.0 (cicd-demo) | 同上 | 关闭，需专项迁移 |
| #366 | bump jest 29.7.0→30.4.2 (perf) | Jest 30 废弃 `--testPathPattern`，改为 `--testPathPatterns` | 关闭，需评估迁移成本 |

### 关闭命令

```bash
gh pr close 371 --comment "Closing: ESLint 8→10 is a breaking change requiring flat config migration. Will handle separately."
gh pr close 373 --comment "Closing: ESLint 8→10 is a breaking change requiring flat config migration. Will handle separately."
gh pr close 366 --comment "Closing: Jest 29→30 is a breaking change (testPathPattern→testPathPatterns). Will handle separately."
```

---

## Draft PR 可标记为 Ready

| PR | 标题 | CI 状态 | 建议 |
|----|------|---------|------|
| #367 | docs(perf): Phase 6 Stage 4 UAT checklist | ✅ All green (13/13 checks) | 标记 Ready for Review，docs-only 变更 |

```bash
gh pr ready 367
```

---

## Breaking Change 迁移建议

### ESLint 8→10 迁移（影响 perf + cicd-demo）

1. 创建 `eslint.config.js` flat config 文件
2. 移除 `.eslintrc.*` 和 `.eslintignore`
3. 更新 `package.json` 中的 ESLint 版本
4. 运行 `npx eslint .` 验证

### Jest 29→30 迁移（影响 perf）

1. `--testPathPattern` → `--testPathPatterns`
2. 检查 `jest.config.js` 中是否有其他废弃 API
3. 验证所有 148 个 unit tests 通过
