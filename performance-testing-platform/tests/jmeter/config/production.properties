# JMeter Production Environment Configuration
# Override base_url, port, and protocol for production target
# Usage: jmeter -n -t smoke.jmx -q config/smoke.properties -q config/production.properties
base_url=api.example.com
port=443
protocol=https
