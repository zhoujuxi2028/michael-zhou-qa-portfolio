# JMeter Dry-run Configuration
# Purpose: 1 thread x 1 loop — verify all requests return expected status codes
# before running full tests (postmortem lesson from #50: productId vs product_id)
#
# Usage: npm run jmeter:dryrun
threads=1
duration=10
rampup=1
base_url=localhost
port=3000
