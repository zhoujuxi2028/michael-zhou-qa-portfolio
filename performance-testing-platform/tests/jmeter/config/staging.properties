# JMeter Staging Environment Configuration
# Override base_url and port for staging target
# Usage: jmeter -n -t smoke.jmx -q config/smoke.properties -q config/staging.properties
base_url=staging.example.com
port=3000
